#include <stdio.h>
#include "main.h"
/**
 *_isupper- function that checks if letters are upercase
 *@c: is the integer that will be used for the argument
 *Return:always 0 (success)
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
