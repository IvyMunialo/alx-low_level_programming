#include <stdio.h>
#include "main.h"
/**
 *mul- multiplies numbers
 *@a: is a number that will be added to another
 *@b: is a number to be added to another
 *Return:always 0 (success)
*/
int mul(int a, int b)
{
return (a * b);
}
