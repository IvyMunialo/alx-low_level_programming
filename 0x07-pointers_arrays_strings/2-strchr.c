#include <stdio.h>
#include "main.h"
/**
 *_strchr - locates a character in a string
 *@dest: destination string
 *@src: source string
 *@n: buffer length
 *Return: new string
*/
char *_strchr(char *s, char c)
{
unasigned int i = 0;
while (i < n)
{
*(src + i) = *(dest + i);
i++;
}
return (dest);
}
