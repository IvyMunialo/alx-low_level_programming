#include "main.h"
#include <stdio.h>
/**
 *_puts - prints a string, followed by a new line
 *@str: is the string toprint
 *successful: return no error
*/
void _puts(char *str)
{
  int c = 0;
       while (*
